<% unless current_user == user %>
  <% if current_user.favorites?(article) %>
    <%= form_with(model: current_user.favorites.find_by(article_id: article.id), local: true, method: :delete) do |f| %>
      <%= hidden_field_tag :article_id, article.id %>
      <%= f.submit 'お気に入り登録を解除', class: 'btn btn-danger btn-sm' %>
    <% end %>
  <% else %>
    <%= form_with(model: current_user.favorites.build, local: true) do |f| %>
      <%= hidden_field_tag :article_id, article.id %>
      <%= f.submit 'お気に入り登録', class: 'btn btn-primary btn-sm' %>
    <% end %>
  <% end %>
<% end %>